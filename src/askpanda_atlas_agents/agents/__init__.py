"""Agent implementations for AskPanDA-ATLAS.

This package contains all agent implementations following the base Agent
interface defined in base.py. Each agent follows a minimal lifecycle:
start() → tick() → health() → stop().
"""

